services:
  osmp:
    build: .
    image: osmp:latest
    container_name: osmp
    command: pipeline
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
